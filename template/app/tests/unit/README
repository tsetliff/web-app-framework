This directory should mirror the src directory and be used to test one small component at a time.  You should be
taking advantage of the fact that all your business logic classes inject dependencies instead of using the new or some
other operator to create instances of classes inside of them.